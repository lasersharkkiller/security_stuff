KnowBe4 modifies the excel file by unzipping, then modifying the xl/drawings/_rels/drawing1.xml.rels and adds an entry for a site, which sends out a little beacon upon opening the excel document. The docProps\core.xml holds the email POC, and xl\sharedStrings holds the POC for returning the drive.
