#Extend space
sudo pvs
sudo pvresize /dev/sda3
df -h #find logical volume name
sudo lvextend -r -l +100%FREE /dev/mapper/ubintu--vg-ububtu--lv

sudo apt install python3-pip
pip3 install transformers torch --cache-dir=Downloads/
pip3 install sentencepiece
sudo apt-get install libportaudio2
